server:
  port: 9000
spring:
  application:
    name: auth-server
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce
    username: root
    password: Allah@2551995
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: "http://localhost:8070/eureka/"
